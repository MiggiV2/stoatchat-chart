{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "stoatchat.serviceAccountName" . }}
  labels:
    {{- include "stoatchat.labels" . | nindent 4 }}
{{- end }}
